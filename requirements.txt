# GlobalSec Platform — Python Dependencies
# All versions are current as of platform v1.0.0 release
# Bumped to address all known CVEs

# === Core HTTP / async ===
aiohttp==3.13.4
httpx==0.28.1
requests==2.32.3

# === Web framework (Paperclip orchestrator + agent health endpoints) ===
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# === Azure SDK (default cloud provider) ===
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0
azure-servicebus==7.13.0
azure-mgmt-resource==23.2.0
azure-mgmt-monitor==6.0.2
azure-monitor-query==1.4.1
azure-mgmt-security==7.0.0
azure-mgmt-securityinsight==2.0.0b2
azure-storage-blob==12.24.0

# === AWS SDK (when GLOBALSEC_CLOUD_PROVIDER=aws) ===
aioboto3==13.3.0
boto3==1.35.85

# === GCP SDK (when GLOBALSEC_CLOUD_PROVIDER=gcp) ===
google-cloud-secret-manager==2.21.1
google-cloud-pubsub==2.27.1
google-cloud-storage==2.19.0
google-auth==2.36.0

# === Cloud-neutral SDKs (when GLOBALSEC_CLOUD_PROVIDER=kubernetes) ===
hvac==2.3.0                                      # HashiCorp Vault
aiokafka==0.12.0                                 # Apache Kafka
miniopy-async==1.21.1                            # MinIO S3-compatible

# === Microsoft Graph (for Entra ID, Defender, Purview, Intune, Sentinel) ===
msgraph-sdk==1.13.0
msgraph-core==1.2.0

# === Data / time-series ===
asyncpg==0.30.0
sqlalchemy==2.0.36
alembic==1.14.0
redis==5.2.1
pandas==2.2.3
numpy==2.2.0

# === Auth / crypto ===
PyJWT==2.12.0
cryptography==46.0.7
python-multipart==0.0.26

# === Reporting / templates ===
jinja2==3.1.6
weasyprint==68.0
markdown==3.7
python-dotenv==1.2.2

# === Chat platforms (Teams primary, Slack secondary) ===
botbuilder-core==4.16.2
botbuilder-schema==4.16.2
slack-bolt==1.21.2
slack-sdk==3.33.4

# === Observability ===
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-aiohttp-client==0.50b0
opentelemetry-exporter-otlp==1.29.0
prometheus-client==0.21.1
structlog==24.4.0

# === Vendor SDKs ===
crowdstrike-falconpy==1.5.2

# === Development / testing (used in CI) ===
pytest==9.0.3
pytest-asyncio==0.25.0
pytest-cov==6.0.0
pytest-mock==3.14.0
black==26.3.1
ruff==0.8.4
mypy==1.13.0
bandit==1.8.0
